name: 📥 ピラティス Feedback 同期 + 学習 (10:00 JST)

on:
  schedule:
    - cron: '7 1 * * *'    # 10:07 JST
  workflow_dispatch:

concurrency:
  group: pilates-feedback
  cancel-in-progress: false

jobs:
  feedback-sync:
    name: ピラティス feedback 全自動処理
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: credentials / .env 復元
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          NOTION_KNOWLEDGE_DB_ID: ${{ secrets.NOTION_KNOWLEDGE_DB_ID }}
          NOTION_INBOX_DB_ID: ${{ secrets.NOTION_INBOX_DB_ID }}
          NOTION_SUCCESS_DB_ID: ${{ secrets.NOTION_SUCCESS_DB_ID }}
          NOTION_SCRIPT_DB_ID: ${{ secrets.NOTION_SCRIPT_DB_ID }}
          NOTION_PARENT_PAGE_ID: ${{ secrets.NOTION_PARENT_PAGE_ID }}
          NOTION_LEADER_FB_DB_ID: ${{ secrets.NOTION_LEADER_FB_DB_ID }}
          SLACK_FEEDBACK_CHANNEL_ID: ${{ secrets.SLACK_FEEDBACK_CHANNEL_ID }}
          SLACK_OWNER_USER_ID: ${{ secrets.SLACK_OWNER_USER_ID }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          OUTPUT_SPREADSHEET_ID: ${{ secrets.OUTPUT_SPREADSHEET_ID }}
          GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          mkdir -p config data output/logs
          printf '%s' "${SLACK_BOT_TOKEN}" > config/slack_bot_token.txt
          echo "${GOOGLE_SERVICE_ACCOUNT_JSON}" | base64 -d > config/credentials.json
          cat > config/.env <<EOF
          NOTION_TOKEN=${NOTION_TOKEN}
          NOTION_DATABASE_ID=${NOTION_DATABASE_ID}
          NOTION_KNOWLEDGE_DB_ID=${NOTION_KNOWLEDGE_DB_ID}
          NOTION_INBOX_DB_ID=${NOTION_INBOX_DB_ID}
          NOTION_SUCCESS_DB_ID=${NOTION_SUCCESS_DB_ID}
          NOTION_SCRIPT_DB_ID=${NOTION_SCRIPT_DB_ID}
          NOTION_PARENT_PAGE_ID=${NOTION_PARENT_PAGE_ID}
          NOTION_LEADER_FB_DB_ID=${NOTION_LEADER_FB_DB_ID}
          SLACK_FEEDBACK_CHANNEL_ID=${SLACK_FEEDBACK_CHANNEL_ID}
          SLACK_OWNER_USER_ID=${SLACK_OWNER_USER_ID}
          SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}
          OUTPUT_SPREADSHEET_ID=${OUTPUT_SPREADSHEET_ID}
          GOOGLE_PLACES_API_KEY=${GOOGLE_PLACES_API_KEY}
          SLACK_BOT_TOKEN_FILE=config/slack_bot_token.txt
          EOF

      - name: 📥 振り返り全自動処理(ログ蓄積つき)
        env:
          TZ: Asia/Tokyo
        run: |
          mkdir -p output/logs
          LOG="output/logs/feedback_$(date +%Y-%m-%d).log"
          {
            echo "===================================="
            echo "📥 feedback $(date '+%Y-%m-%d %H:%M:%S') (GitHub Actions)"
            echo "===================================="
            echo ""
            echo "📨 [1/8] Slack→Notion同期"
            python -u src/slack_fetcher.py || echo "❌ slack_fetcher 失敗(継続)"
            echo ""
            echo "💎 [2/8] リーダーFB抽出→Notion蓄積"
            python -u src/leader_fb_extractor.py || echo "❌ leader_fb_extractor 失敗(継続)"
            echo ""
            echo "📥 [3/8] 受信箱処理"
            python -u src/inbox_processor.py || echo "❌ inbox_processor 失敗(継続)"
            echo ""
            echo "✨ [4/8] 成功事例検出"
            python -u src/success_detector.py --days 7 || echo "❌ success_detector 失敗(継続)"
            echo ""
            echo "⭐ [5/8] 口コミ取得(Google + HPB)"
            python -u src/reviews_fetcher.py || echo "❌ reviews_fetcher 失敗(継続)"
            echo ""
            echo "📊 [6/8] ダッシュボード更新"
            python -u src/full_dashboard_updater.py --daily || echo "❌ full_dashboard_updater 失敗(継続)"
            echo ""
            echo "👥 [7/8] 解約集計_スタッフ別 更新"
            python -u src/cancel_staff_aggregator.py || echo "❌ cancel_staff_aggregator 失敗(継続)"
            echo ""
            echo "🧮 [8/8] 個別契約率シート更新(月初1-5日のみ・前月分)"
            python -u src/personal_contract_updater.py --auto || echo "❌ personal_contract_updater 失敗(継続)"
            echo ""
            echo "完了: $(date '+%H:%M:%S')"
          } 2>&1 | tee -a "$LOG"

      - name: 📩 振り返り蓄積完了通知 → 松崎さんDM (同ランナー内・ログ確実)
        env:
          TZ: Asia/Tokyo
        run: python -u src/notify_after_sync.py
        continue-on-error: true

      - name: ログ確認
        if: always()
        run: |
          ls -la output/logs/ 2>/dev/null || true
          tail -100 output/logs/feedback_*.log 2>/dev/null || true
